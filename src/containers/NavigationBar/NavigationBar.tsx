import styles from "../../style.tsx";

const NavigationBar = () => {
  return (
      <nav className={`${styles.NavBase}`}>
        <div className=""></div>
      </nav>
  );
};

export default NavigationBar;
